package eu.toop.connector.me;

/**
 * @author yerlibilgin
 */
public enum SignalType {
  /**
   * The signal is a RECEIPT signal (the notification is a success notification)
   */
  RECEIPT,
  /**
   * The signal is an Error Signal (the notification indicates an error)
   */
  ERROR
}
